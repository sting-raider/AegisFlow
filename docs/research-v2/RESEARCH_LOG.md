# Detector v2 research log

Append-only. Failed and inconclusive experiments remain visible. No frozen v1 evidence
feeds any decision recorded here.

## MR2-000 — Namespace, boundary, capability audit

Date: 2026-08-22

Status: complete

Created `codex/detector-v2` and the v2 namespace (`docs/research-v2/`,
`configs/research-v2/`, `training/v2/`, `packages/detection_v2/`). v1 evidence files are
untouched. The predeclared protocol is in `docs/research-v2/MASTER_PLAN.md` with fixed
objectives, the eight-source IoT-23 PCAP development pool, and a reserved final
environment (CTU-13 scenario 8 rbot; fallback CTU-IoT-Malware-Capture-43-1) decided
before any model fitting.

Packet-sequence capability audit of the runtime contract (`FlowEvent`, schema 1.0.0):

| Path | first-N sizes | directions | IATs | TCP flag counts | Notes |
|---|---|---|---|---|---|
| Scapy PCAP adapter (`services/sensor/adapters.py`) | yes, real packets <= 20 | yes, semantic initiator/responder | yes, ms, first entry 0.0 | yes, per-packet counts (S/A/F/R/P) | full sequence from actual packets |
| NFStream adapter (PCAP + live) | yes, SPLT <= 20 (`splt_analysis=20`) | yes, src2dst remapped to semantic direction | yes, PIAT ms | yes, bidirectional counts | native engine Linux-only |
| Suricata EVE | inherits sensor values | corrects/flips on orientation fix | inherits | inherits | EVE never fabricates flows |
| Demo sensor | synthetic 3 packets | synthetic | synthetic | none | installation fixture only |
| CSV dataset adapters | **unavailable** | n/a | n/a | partial columns | sequences cannot be invented for CSV-only sources |

Machine-readable matrix: `configs/research-v2/capability-matrix.json`.

Consequence: v2 development data must be generated by replaying official PCAPs through
the same adapters the runtime uses, guaranteeing training/inference parity for the
sequence representation. The eight chosen IoT-23 scenarios have per-flow ground truth
in their official Zeek `conn.log.labeled` files, enabling label joins without inventing
labels.
