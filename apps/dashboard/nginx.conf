resolver 127.0.0.11 valid=10s ipv6=off;

upstream aegisflow_api {
  zone aegisflow_api 64k;
  server api:8000 resolve;
}

server {
  listen 8080;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  add_header X-Content-Type-Options "nosniff" always;
  add_header Referrer-Policy "no-referrer" always;
  add_header Content-Security-Policy "default-src 'self'; connect-src 'self' ws: wss:; style-src 'self' 'unsafe-inline'; script-src 'self'; img-src 'self' data:;" always;

  location /api/ {
    proxy_pass http://aegisflow_api;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
  }

  location /health/ {
    proxy_pass http://aegisflow_api;
  }

  location / {
    try_files $uri /index.html;
  }
}
